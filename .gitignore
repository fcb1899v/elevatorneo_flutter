# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Web related
lib/generated_plugin_registrant.dart

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android build files
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/gradle-wrapper.jar
android/gradlew
android/gradlew.bat

# Android CMake and C++ build files
android/app/.cxx/
android/app/build/
android/app/src/main/cpp/build/
**/CMakeCache.txt
**/CMakeFiles/
**/cmake_install.cmake
**/build.ninja
**/rules.ninja
**/*.ninja
**/compile_commands.json

# iOS build files
ios/build/
ios/Pods/
ios/Podfile.lock
ios/Flutter/ephemeral/
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh

# Generated files
*.g.dart
*.freezed.dart
*.mocks.dart

# Environment files
.env
.env.local
.env.production

# Dart files
# Not admob_banner.dart / admob_interstitial.dart: they are the ad
# implementation itself. Unit IDs come from .env and constant.dart, so these
# files hold nothing secret. Both are already tracked; the rule only meant
# that dropping them from the index would have made them unrecoverable.
# Not firebase_options.dart: the build needs it, and every value in it
# (apiKey, appId, projectId, messagingSenderId, storageBucket, and the
# iOS/Android client ids) ships inside the app binary, so hiding the file
# hides nothing from anyone who has the app. A Firebase API key identifies
# the project; it does not authorize access. Firestore rules and App Check
# do that.

# Keystore files
**/*.keystore
**/*.jks
key.properties

# Firebase configuration files (if they contain sensitive data)
# Not google-services.json / GoogleService-Info.plist: this app applies the
# com.google.gms.google-services Gradle plugin, which fails the Android build
# when the json is absent, and six of the eight Xcode projects list the plist in
# their Resources build phase. Both files hold the same identifiers as
# lib/firebase_options.dart and ship inside the app, so excluding them hid
# nothing and cost two apps their copy (kawaii and letswashlet, 2026-09-02).

# Coverage files
coverage/
*.lcov

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test
.env.production

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# next.js build output
.next

# nuxt.js build output
.nuxt

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port
assets/.env
ios/firebase_app_id_file.json

# --- Tooling config that belongs to the workspace, not to this app ---
.claude/
.cursorignore

# --- Flutter iOS build output (SPM checkouts, generated env) ---
ios/Flutter/ephemeral/

# --- Build output and generated plugin registrants (regenerated by pub get) ---
.flutter-plugins
.flutter-plugins-dependencies
ios/Podfile.lock
ios/.symlinks/
ios/Flutter/.last_build_id
ios/build/
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
**/GeneratedPluginRegistrant.*
**/generated_plugin_registrant.*
**/xcuserdata/

# --- Signing assets (added 2026-09-06) ---
# None of these are in any repository today. The rules exist so that a
# certificate exported to the project directory for a one-off cannot be
# committed by a later `git add -A`. Distribution certificates and the App
# Store Connect key grant publishing rights, and a leak is not fixed by
# deleting the file: the history keeps it.
*.p12
*.p8
*.cer
*.certSigningRequest
*.mobileprovision
*.provisionprofile
